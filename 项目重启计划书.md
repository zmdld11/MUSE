# Music Unmixing & Score Extraction System

**简称：**MUSE

**名字来源：**Unmixing 是音频源分离领域的行业通用术语，精准对应核心分离功能；Score Extraction 完整覆盖乐器识别 + 自动乐谱生成全流程。MUSE 本身是希腊神话中的音乐与艺术女神，天然绑定音乐属性，适配全场景，辨识度拉满。

小孩之前不懂事，把项目拆成了几个小项目分开做，现在发现为什么不合起来做呢？实在是蠢完了。正好这次学习一下怎么用VSCode的工作区。

## 项目说明

这是一个主要功能是调用音频源分离模型对吉他音轨进行分离的应用系统，后面可能会适配更多的乐器。最开始参考的模型是Demucs，而且我看了3遍他们的README.md都没有发现他们支持吉他，我一直以为他们只有人声、鼓、贝斯和其他。结果上周做论文整理的时候才发现他们原来还有一个扩展模型，支持钢琴和吉他。想到我的吉他模型训了半天SDR还是负的，瞬间心凉了。不过这也好，万一我后面真的训不出来还有一条退路。

目前我的构想里这个项目由以下四个部分组成：

1. **乐器识别模型：**可以识别多种常见的乐器出现在某个音频文件的哪些部分，也可以指定乐器进行单乐器识别。这个是我最开始做的测试项目，后来发现好像可以借用到音轨分离模型里优化，因此就作为一个独立项目保留下来，既可以提升音轨分离模型质量，也可以显得我这个系统功能很丰富。
2. **音轨分离模型：**整个项目的中心，目前的主要功能就是将吉他音轨分离出来，后续有条件可以增加更多乐器的适配。
3. **乐谱生成模型：**根据分离出来的音轨音频自动拾取音高并生成相应的乐谱文件。
4. **前端交互界面：**实现系统的交互操作。

## 项目环境

**本机：**INTEL core I9-14900HX + NVIDIA 4060 LAPTOP 8GB + 64GB RAM + 4T SSD

**AutoDL：**由于预算受限，GPU最高可选5090 32GB

**数据集：**均来源于网络

1. IRMAS-TrainingData（混轨数据集，用来训练最初版本的乐器识别模型）
2. nsynth（分轨数据集，提供了每种乐器的单音，试验下来不能进行模型训练，但是可以生成MIDI音频）
3. MedleyDB（分轨数据集，有每个乐曲的分轨音频，可用来训练分轨模型）
4. moisesdb_v0.1（同上）

**Python版本：**3.10~3.14均可使用，对于每个部分用conda进行版本隔离。（先前训练用的conda环境还在，可以直接使用，但是我更倾向于重新创建环境）

## 重做计划书

### STEP1：结构重组

由于原来我的项目把乐器识别模型和音轨分离模型拆开来训练，他们两个是各自独立的工作区，随着训练集一点点增加，发现再下去会浪费很多无谓的空间，模型间的联动也会极其困难。因此要重构项目的结构。以下是我构想的项目结构：

```txt
MUSE/
- data/		#数据集存放目录
- 乐器识别模型工作区/
- - markdown/	#放训练过程中各种markdown说明文档
- - src/		#模型训练代码
- - test/		#模型测试代码（这个文件夹里的代码是用来人工测试的，机器自动测试模型放在src）
- - model/		#存放pth文件
- - - log/		#训练日志
- - output/		#调用模型后的各种输出
- 音轨分离模型工作区/
- 乐谱识别模型工作区/
- 前端工作区/
- README.md
```

整个项目的数据集都放在data文件夹里共享，减少没必要的空间开销。

上面每个工作区的文件夹名我没想好，帮我取一下。

### STEP2：设置SKILL

对于整个项目的代码编写，我有以下需求：

```txt
1. 每次修改完代码，都要先运行调试。模型训练代码需要能够稳定训练1epoch，测试代码需要能稳定运行不会出错。没有问题以后再将代码交给我。
2. 每次修改模型结构需要记录下当前模型的结构、尝试创新点和最终成果，记录在各自工作区的markdown/model_log.md中，如果没有就创建文件。修改模型前更新上一个模型的性能评价，并编写当前版本的新模型结构。修改的同时给每个模型进行版本编号，比如VER3.1等，同时创建相应的版本分支。
3. 修改模型后push上GitHub的对应分支。怎么push指定的工作区我还不是很清楚，你能给我个方案最好。因为我不可能所有模型一起做一起改，肯定是一个一个做过来修改过来。
```

对于模型训练我也有通用的要求：

```txt
1. 模型训练过程中要有可视化进度条。
2. 每个epoch不能超过5分钟，不然训练太没有效率了。
3. 每次训练时在model/log/中创建一个“时间戳.log”的文件，将每个epoch的结果输出到.log文件夹中。
4. 模型需要支持断点训练。毕竟这是个大模型。
```

